pipeline {
    agent {
        label "jenkins-slave-skopeo"
    }
    options {
        skipDefaultCheckout()
        disableConcurrentBuilds()
    }
    stages {
        stage("Initialize") {
            steps {
                script {
                    sh "skopeo -h"
                }
            }
        }
    }
}